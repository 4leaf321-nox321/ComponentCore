# 로드맵

2026-09-18 에 정한 순서. 단계마다 **무엇을 만들고, 무엇이 되면 끝인지**를 적는다. 진행하면서
바뀐 것은 여기 고친다 — 이 문서가 「다음에 무엇을 하나」 의 정본이다.

## 구조 — 내 공간에서 그려서, 승격으로 내놓는다 (2026-09-18 에 다시 세움)

```
내 활동
  새 작업 ────▶ 내 작업 (내 공간 · 나만 봄 · 형상 버전 + 지그 생성 기록)
                   │ 승격                       ▲ 내 공간으로 복사
                   ▼                            │
공용        부품 카탈로그  ◀── 참조 ──  지그 카탈로그
            (불변 버전)                (불변 버전 · 어느 부품 버전의 지그인가)
```

| 화면 | 뜻 | 코드 |
| --- | --- | --- |
| 새 작업 | 빈 화면 · 템플릿 · 기존 작업의 사본 · STEP 에서 부품이나 지그를 그려 **새 작업으로만** 저장한다. 덮어 저장은 내 작업 › 수정의 일. 저장 전엔 아무것도 안 남는다 | `works/DrawPage` |
| 내 작업 | 그리고 있는 문서. 부품(레시피 버전) + 지그(옵션 · 실행 기록). **나만 본다**. 카탈로그의 부품 · 지그와 같은 말 — 다른 건 공개 여부뿐 | `works/*` |
| 실행 기록 | 내가 건 작업(형상 평가 · 지그 생성)과 산출물 | `jobs/*` |
| 템플릿 | 새 작업의 출발점. **내 것 · 공용** 두 자리, 남의 것은 「내 것으로 복사」 | `templates/*` |
| 부품 | 승격된 형상. 누구나 보고 「내 공간으로 복사」 | `parts/*` |
| 지그 | 승격된 지그 + 어느 부품 버전의 지그인가 | `jigs/*` |

- 올린 STEP 도 `import_step` 노드 하나짜리 **버전**이다 — 제품은 늘 「이 작업의 형상」 하나다.
- 승격은 복사다. 카탈로그의 버전은 바뀌지 않는다. 고치려면 내 작업에서 고쳐 다시 승격(v2).
- 지그를 승격할 때 제품(그때의 형상 버전)이 부품에 없으면 함께 올린다 — 지그만 있는 카탈로그는
  반쪽이다.
- 권한: 내 작업과 그 작업물은 소유자 · 관리자만. 승격된 작업물은 누구나(`jobs.require_visible`).
- 이전 구조(CAD 모델 · 지그 프로젝트)는 마이그레이션 0004 가 내 작업으로 옮겼다.

## 네 개의 명사

| 사람의 말 | 플랫폼의 명사 | 뜻 |
| --- | --- | --- |
| 작업 하나하나 | **작업(Job)** | 입력 + 옵션 → 실행 → 산출물. 종류(kind)가 있다: jig · cad · ai_cad … |
| 작업한 것의 목록 | **작업물(Artifact)** | 파일 하나. 어느 작업이 무엇으로 만들었나(계보)를 안다 |
| 내 공간 / 공용 / 외부 API | **소유 · 공개 · 게시(Publish)** | private → shared → published(고정 ID · 불변 버전) |
| CAD 를 손으로도 AI 로도 | **모델(Model) = 레시피** | 형상을 만드는 연산 트리(JSON). 손그리기와 AI 가 같은 것을 만진다 |

**레시피가 핵심 결정이다.** CAD 를 메시 · STEP(결과)이 아니라 레시피(만드는 법)로 저장한다.
그래야 (1) 치수를 바꿔 다시 만들고 (2) 손으로 그린 것을 AI 가 고치고 그 반대도 되고 (3) AI 가
만든 것을 평가기로 만들어 보고 검증하고 (4) 버전 차이가 "fillet 3 → 5" 처럼 뜻을 갖는다.
외부 CAD 에서 온 STEP 은 `import_step` 노드로 레시피의 뿌리가 된다 — 처음부터 파라메트릭으로
만들라는 뜻이 아니라, **이 플랫폼에서 가한 변경을 기록하는 방식**이다.

## 1단계. 작업 · 작업물 · 워커 — 나머지 전부의 바닥 ✅ 완료 (2026-09-18)

왜 먼저: 지그 생성 · CAD 생성 · AI 생성 · 게시가 전부 "작업" 이다. 이게 없으면 기능마다 실행 ·
상태 · 목록을 따로 만든다.

- 표 `jobs`(kind · project · input · options · status · progress · summary · error), `artifacts`
  (job · kind · 파일 · 크기 · 부모 artifact)
- `JigRun` 을 `Job(kind="jig")` 로 흡수. 결과 파일이 `artifacts` 행이 된다.
- **워커** `python -m app.worker` — DB 를 큐로 쓴다(`FOR UPDATE SKIP LOCKED`). 별도 큐 서버 없음.
  API 는 즉시 `queued` 로 답하고 화면은 폴링. 개발에서는 `run.py` 가 워커를 자식으로 띄운다.
  `JOBS_INLINE=1` 이면 요청 안에서 동기로 돈다(시험 · 워커 없는 작은 설치).
- 화면: 「내 작업」 목록(상태 · 종류 · 산출물), 프로젝트 화면은 폴링으로 진행 단계를 보여 준다.
- 완료 기준: 지그 생성을 눌러도 화면이 안 멈추고, 목록에서 지난 작업의 STEP 을 다시 받는다.
- 남긴 것: 작업 취소 · 워커 상태(살아 있나)를 서버 화면에 — 필요해질 때.

## 2단계. CAD 레시피 — 모델의 표현 ✅ 완료 (2026-09-18). CAD 레시피 — 모델의 표현

- `core/recipe/` — JSON 연산 트리 + build123d 평가기. 연산: sketch(rect · circle · polygon ·
  line/arc) · extrude · revolve · cut · union · fillet · chamfer · hole · pattern · import_step · transform
- 표 `models`, `model_versions`(레시피 · 출처 손/AI · 메모). 평가는 Job(kind="cad").
- 지금의 `primitives.py` 4종은 레시피 템플릿이 된다. 지그 프로젝트의 「제품」 이 model 을 가리킨다.
- 완료 기준: 레시피를 POST 하면 STEP 이 나오고, 치수만 바꿔 새 버전을 만든다.
- 된 것: `core/recipe`(schema · evaluate · templates), `cad_models` · `cad_model_versions`,
  `Job(kind="cad")`, 미리보기 · 검증 · STEP API, 모델 목록 · 상세 · 버전 이력 · 되돌리기 화면,
  지그 프로젝트의 제품으로 모델 선택. 편집은 아직 JSON 텍스트(3단계가 폼 · 캔버스로 바꾼다).
- 남긴 것: `import_step` 노드를 화면에서 쓰는 길(STEP 업로드 → 작업물 → 레시피 뿌리) — 3단계에서
  「STEP 위에 구멍 하나 더」 를 할 때 붙인다.

## 3단계. 직접 그리기 — 레시피 편집기 ✅ 완료 (2026-09-18). 직접 그리기 — 레시피 편집기

1. 피처 트리 편집기(노드 목록 · 치수 폼 · 추가/삭제/순서) + 바꿀 때마다 glTF 미리보기
2. 2D 스케치 캔버스(SVG): 사각형 · 원 · 다각형 · 선/호, 치수 입력, 격자 스냅
3. 3D 에서 고르기: 면 클릭 → 그 면에 스케치, 엣지 클릭 → 필렛 (면 · 엣지 인덱스 ↔ glTF 대응표)
4. 실행 취소 = 버전 되돌리기
- 완료 기준: 마우스와 폼만으로 구멍 뚫린 L 브래킷을 처음부터 그려 STEP 으로 받는다.
- 된 것: 피처 트리 + 노드별 폼(`cad/recipeSpec.ts` 가 서버 스키마와 짝, 시험이 대조) · 스케치
  SVG 캔버스(놓기 · 끌기 · 격자 스냅 · 치수 폼) · 자동 미리보기(면 · 엣지 단위 메시
  `/cad/recipe/mesh`) · 3D 에서 면을 누르면 「그 면 위에 스케치」(PlaneSpec.normal) · 엣지를 누르면
  필렛/모따기 대상(EdgeNear — 인덱스가 아니라 중점 위치라 형상을 조금 고쳐도 살아남는다) ·
  id 를 바꾸면 참조가 따라옴 · 지우면 그것을 쓰는 노드도 함께 · JSON 토글.
- 더한 것(2026-09-18 저녁): 상단 가로 툴바(피처 종류별) · 피처 편집은 모달 · 3D 넓게 · 빈 레시피
  에서 시작 · 템플릿 저장(내 것/공용) · **전체 화면**(Fullscreen API, 포털이 그 안에 붙게) ·
  **측정**(꼭짓점 스냅 점 · 엣지 길이 · 면 넓이/법선 · 두 점 거리 ΔXYZ · 세 점 각도) ·
  피처: 임의 윤곽(선/호) · 규격 구멍(카운터보어 · 카운터싱크 · 탭 · M3~M12 · 임의 면) · 쉘 · 격자
  패턴 · 로프트 · 구 · 원뿔 · 토러스.
- 더한 것(2026-09-18 밤): 툴바를 **리본**으로 — 탭(파일 · 스케치 · 입체 · 조합 · 마감 · 배치 ·
  보기·측정)마다 아이콘 단추. 「파일」 탭에 새로 · 레시피 불러오기(내장/저장 템플릿) · 기존 작업
  불러오기(레시피 복사) · 저장(내 작업으로 / 새 버전으로) · 템플릿 · STEP · JSON 을 모았다 —
  그리기 화면의 템플릿 고르기 셀렉트는 없앴다. 모달은 여백 · 손잡이 어디서든 끌리고 전체 화면
  안에서도 같다.
- 더한 것(2026-09-19): 이름을 MCAD 말로(블렌드 · 챔퍼 · 결합 · 교차 · 미러 · 블록 · 원통) ·
  **스윕**(3D 점 경로, 직선/스플라인) · 돌출 **구배** · 스케치 **타원 · 글자**(각인/양각) ·
  **자르기**(평면, 위/아래/둘 다) · **여유**(offset — 제품을 키워 빼면 포켓) · 이동에 배율 ·
  **실행 취소/다시 실행**(Ctrl+Z/Y, 1초 안 잇단 변화는 한 걸음) · 평면 칸마다 「3D 에서 면 고르기」.
- 더한 것(2026-09-19, 이어서): 돌출 **「다음 면까지 · 마지막 면까지(관통)」**(대상 입체 지정) ·
  **나선**(스프링 · 나사산, 단면 자동 배치) · **단면**(입체를 평면으로 자른 스케치 + 여유 → 포켓
  윤곽) · 스케치 **윤곽 여유**(2D 오프셋).
  시도했다가 접은 것: 실루엣(그림자 윤곽) — build123d `project` 가 곡면에서 엉뚱한 면을 내놓는다
  (실측: 누운 원통이 137mm 사각형). HLR 로 다시 할 때까지 「단면 + 여유」 로 대신한다.
- 더한 것: 스케치 **선(두께)** — 중심선을 찍고 폭을 주면 리브 · 얇은 벽(열린 와이어 offset →
  make_face). 사각형을 선으로 가르면 Sketch 불리언이 Compound 를 내놓는다 → 면을 다시 Sketch 로.
- 더한 것(2026-09-19, 셋째): **측정을 3D 에 그린다** — 번호 · 길이 · 넓이 · 치수(점선) · 각도를
  스프라이트 글자로, 고른 엣지는 빨간 선 · 고른 면은 반투명 빨강(가려져도 보이게 depthTest off).
  뷰어에 **표준 뷰**(등각 · 정면 · 윗면 · 우측). 피처: **쐐기**(경사 블록) · **구배**(면 기울이기) ·
  스케치 **둥근 사각형 · 사다리꼴 · 모서리 둥글리기 · 볼록 윤곽(hull)**. 내보내기 **STL**(3D 프린트) ·
  **DXF · SVG**(2D 도면 — 입체면 높이 절반의 단면을 눕혀서).
- 더한 것(2026-09-19, 넷째 — **AI 가 말로 받은 치수를 그대로 옮기게**): 구간의 호를
  `radius`(부호가 휘는 쪽) · `tangent`(앞 구간에 매끄럽게)로 — `via` 는 사람이 찍는 칸으로 남긴다 ·
  장공 `measure: centers`(도면이 주는 중심 사이) · `triangle`(변 · 각 셋) · **판금 절곡**
  (`sheet_metal` — 꺾은선 · 두께 · 폭 · 굽힘 R, 꺾은선이 폭의 가운데).
- 더한 것(2026-09-19, 다섯째): **템플릿을 제 공간으로** — `modules/templates`(모델 · 서비스 ·
  라우터 · 화면)로 옮기고 `/api/templates` 로 뺐다. 자리는 내 것 · 공용, 스위치 하나로 오간다.
  목록은 레시피 본문을 싣지 않고(`node_count` 만) 고를 때 받는다 · 찾기 · 쪽 나누기 ·
  「내 것으로 복사」 · 「그리기에서 열기」(`/draw?template=<id>`). MCP 에 `template_recipe` ·
  `save_template`.
- 더한 것(2026-09-19, 여섯째): 피처 트리에서 **손을 올리면 고치기 · 지우기 단추**가 뜨고,
  **끌어서 순서를 바꾼다**. 선후관계(쓰는 피처 ↔ 쓰이는 피처)는 놓기 전에 막고 이유를 적는다 —
  규칙은 `cad/reorder.ts`(순수 함수 + 단위 시험), 화면은 놓을 자리를 선으로만 표시한다.
  모달의 「앞으로 · 뒤로」 도 같은 규칙으로 막힌 쪽이 꺼진다.
- 고친 것: 「STEP 파일에서 시작」 을 화면 머리에서 **「파일」 탭 시작 그룹**으로(불러오기가 한자리에),
  「전체 화면」 은 탭 안이 아니라 **리본 오른쪽 위**로(어느 탭에서나 바로 키운다).
- 더한 것(2026-09-19, 일곱째): **측정 고도화**. 창을 띄운 채 3D 를 계속 누르는 **막 없는 모달**
  (`Dialog modal={false}` + `DialogContent overlay={false}`), 고를 종류 칩(점 · 선 · 면),
  하나만 골라도 길이 · 넓이 · **지름**, 둘이면 점↔점 · 점↔선 · 점↔면 · 선↔선 · 선↔면 · 면↔면을
  **한꺼번에**(거리 · 축별 차 · 사잇각 · 나란한가 · 구멍이면 중심 사이 피치), 셋이면 각도.
  「담기」 로 쌓으면 3D 에 남아 여러 곳을 한 화면에서 비교한다. 계산은 `cad/measure.ts` 순수 함수
  (곡면 · 곡선은 근사라고 `≈` 로 밝힌다) · 표시는 `cad/measureMarks.ts`.
  메시가 원 · 원통의 **반지름 · 중심 · 축**을 함께 준다 — 지름을 점 찍어 어림하지 않게.
- 더한 것: 잰 자리를 **자(치수선)** 로 그린다 — 보조선 · 화살표 · 값이 형상 옆으로 비켜서
  (중심에서 먼 쪽) 선다. 고른 선은 **굵은 선**(Line2 — `THREE.Line` 의 굵기는 브라우저가 무시해
  1px 이었다), 고른 점은 **흰 테를 두른 빨간 구**, 손이 올라간 점은 커지며 **주황 고리**를 쓴다.
  담아 둔 측정은 회색으로 옅게 — 지금 재는 것과 구별된다.
- 고친 것: 점을 **눈에 보이는 파란 점**으로 띄우고 그것을 직접 누르게 했다(꼭짓점 · 모서리 중점 ·
  원 중심, 손을 올리면 커진다). 보이지 않는 것에 스냅되기를 기다리게 하지 않는다. 점은 엣지 ·
  면보다 먼저 잡힌다. 오른쪽에 붙인 모달이 가로로 안 움직이던 것도 고쳤다 — 드래그 여백을 네 쪽
  모두 준다(`right` 로 선 모달은 marginRight 로만 움직인다). 형상 · 표시 · 점을 다른 자리에 담아,
  레시피가 바뀌어도 측정 표시가 떨어져 나가지 않는다.
- 더한 것(2026-09-19, 여덟째 — **파라메트릭**): 레시피에 `params`(이름 붙인 치수)를 두고 어느
  숫자 칸에든 `"=판_길이 - 15"` 로 쓴다. 식은 **계산기**다(이름 · 숫자 · 사칙연산 · 괄호 ·
  흰 목록 함수) — `eval` 이 아니라 AST 를 걸러 돌린다. 치수끼리 서로 가리켜도 풀고, 고리면
  그렇게 말한다. 화면 왼쪽에 「치수」 판(이름을 바꾸면 그것을 쓰는 식도 따라 바뀐다), 칸마다 `fx`.
  **「한쪽을 고정하고 반대쪽을 늘린다」** 는 `align`(도형 2축 · 기본 입체 3축, min|center|max)이
  맡는다. 피처 id 와 치수 이름에 **한글**을 쓸 수 있다.
  남는 것: 스케치 구속 솔버(치수 · 평행 · 접함) — 치수 이름 + 기준 자리로 지그에 필요한 만큼은
  되므로 뒤로 미룬다.
- 더한 것: **치수표**(`core/recipe/digest.py` · `POST /cad/recipe/geometry`) — 크기 · 평면(법선 ·
  넓이) · 원통 · **구멍(지름 · 중심 · 깊이 · 축)**. 사람의 측정 창에 해당하는 것을 AI 에게 준다.
  원 모서리를 **같은 축 위에서** 짝지어야 나란한 두 구멍이 한 구멍으로 묶이지 않는다(실측).
  MCP: `recipe_geometry` · `work_geometry` · `part_geometry`(치수표 + 레시피 + **STEP 작업물 id**)
  — 부품을 기준으로 지그를 그릴 때 그 id 를 `import_step` 에 넣어 제품을 불러오고, `offset` 으로
  여유를 줘 블록에서 빼면 포켓이 된다. 가이드에 그 흐름을 적고 시험이 도구 목록을 지킨다.
- 더한 것(2026-09-19, 아홉째 — **공진에 맞추는 시험 지그**를 할 수 있나 살핀 결과):
  - 질량 · 무게중심 · **관성 모멘트**(무게중심 기준)를 치수표에 — `material` 을 주면 나온다.
  - **치수 훑기**(`POST /cad/recipe/sweep`, MCP `sweep_parameter`) — 치수 하나를 값마다 바꿔
    만들어 보고 치수표를 나란히. 실패한 값도 이유와 함께 남는다.
  - **보 1차 공진 가늠값**(`core/vibration.py`, `POST /cad/beam-frequency`) — 두께→Hz 와
    목표 Hz→두께(이분법). 균일 직사각형 보 · 완전 고정 가정의 닫힌 식이라 ±10% 는 흔하다.
    응답에 가정 · 경고 · 「가늠값」 을 실어 보내고, 가이드와 시험이 그 말을 지킨다.
  - 확인한 것: 연결부를 **숫자로 못 박고** 튜닝부만 `params` 로 두면, 두께를 3~10 mm 로 훑어도
    구멍 자리(연결부)는 한 자리도 안 움직인다 — 이 설계가 되는 근거다.
- **모달 해석은 없다**(가장 큰 빈 자리). 붙이려면: `gmsh`(pip 로 설치됨 — 망 확인) 로 4차
  사면체 메시 → `scikit-fem` + `scipy.sparse.linalg.eigsh` 로 K · M 고유값, 또는 CalculiX(ccx)
  바이너리. sudo 없이 되는 길은 앞의 것. 새 job kind `modal`(경계조건 = 고정할 면 고르기)이
  필요하다. **사용자에게 물어보고 시작한다** — 플랫폼의 성격이 바뀌는 크기다.
- 더한 것(2026-09-19, 열째 — **실험계획(DOE)**): 치수 범위에서 형상을 여러 벌 만들어
  **공유 폴더**(`DOE_EXPORT_ROOT`, 기본 `F:\data\0_Program\73_AutoJigGenerator`)에 쏟는다.
  해석(ANSYS)은 그 폴더를 그대로 읽는다.
  - `core/doe.py`(순수): 전체 조합 · LHS(**시드**로 재현) · 개수 미리보기 · 부등식 필터.
    한 번에 만드는 설계점 상한은 **관리자가 화면에서**(서버 › 설정, `server_settings` 표,
    `DOE_MAX_POINTS` 가 .env 기본값 200) 바꾼다 — 점마다 형상 평가 + STEP 쓰기라 상한이 없으면
    한 요청이 서버를 오래 잡는다. 격자가 넘치면 LHS 로 표본 수를 정한다.
    값은 인자마다 **가공 단위**(`resolution`, 기본 0.1 mm · 1 / 0.5 / 0.1 / 0.05 / 0.01)로 맞춘다
    — 구간을 셋으로 나눈 0.333 은 가공할 수 없다. 맞추다 겹친 값은 하나만 남긴다.
    재료 · 질량 · 크기는 **뺐다**(2026-09-19): 표에는 바꾼 변수와 파일 이름만 적는다. 결과는
    해석이 내고, 그것을 설계점에 붙이는 것이 다음 일 — `DoePoint.metrics` 와 필터 · 파레토
    (`/filter` · `/tradeoff` · MCP `doe_tradeoff`)는 그 자리를 위해 남겨 두었고 지금은 빈 답을 낸다.
    화면의 필터 · 트레이드오프 조각은 걷었다.
    규칙은 MechanicalDesign 의 doeEngine 에서 가져왔다 — 빈 칸을 0 으로 읽지 않는 것까지.
  - `modules/doe`: 스터디(레시피 **스냅샷** + 인자 + 시드) · 설계점 · job kind `doe`.
    **실패한 점에서 멈추지 않는다** — 줄에 사유를 적고 다음으로 간다.
  - 폴더 한 벌 = `manifest.csv`(번호 · 상태 · 바꾼 변수 · 파일 · 사유) + `study.json`(다시 만들 때) +
    `README.txt` + `points/p0001.step…`. 화면에는 `F:\…` 로 보여 준다(서버는 `/mnt/f/…`).
  - 화면: 독립 `/doe` 와 **내 작업 › 실험계획 탭**(같은 조각을 쓴다). 만들기 전 개수 확인,
    결과 표 · 부등식 필터 · CSV · 경로 복사.
  - MCP: `doe_preview` · `doe_create` · `doe_points` · `doe_studies`.
- 고친 것(사용자가 못 찾은 것들): 「치수」 → **「변수」**(값에 이름을 붙인다는 뜻이 드러난다).
  변수 만들기 줄을 **이름 + 값 + 「만들기」** 로 바꾸고 다음에 할 일(피처를 열어 fx)을 적었다.
  **스케치 도형의 치수도 변수로** — 피처 칸만 되던 것을 공용 `NumberField` 로 합쳤다.
  캔버스가 `"=두께"` 를 그리려면 값을 알아야 해서 `cad/expr.ts`(미리보기 전용 파서)를 두었다 —
  정본은 서버다. 자바스크립트의 `\w` 는 ASCII 뿐이라 한글 이름이 캔버스에서만 안 풀렸다(실측).
- 고친 것: `fx` 가 **글자 칸을 여는 단추**였다 — 사용자가 이름을 쳐 넣었더니 그런 변수가 없어
  막혔다(만드는 곳은 왼쪽 상자라는 걸 아는 사람만 쓸 수 있었다). 이제 `fx` 를 누르면 **고르거나
  만드는 자리**가 열린다: 있는 변수 목록 · **이 칸의 값을 그대로 새 변수로**(이름만 적는다) ·
  식 직접 쓰기 · 숫자로 되돌리기. 값을 쓰는 자리에서 변수가 생긴다.
- 고친 것: 저장이 한 방향뿐이었다 — 그리기의 「저장」 은 늘 **새 작업**, 내 작업의 「저장」 은 늘
  **같은 작업에 새 버전**. 기존 작업을 불러와 고친 뒤 저장하면 사본이 생기고, 내 작업에서는 사본을
  만들 길이 없었다. 이제 양쪽 다 **「〈이름〉 에 덮어 저장」 / 「새 작업으로 저장」** 두 갈래다.
  덮어 저장은 새 버전을 붙이는 것이라 옛 버전이 남는다. 불러온 작업이 있으면 덮어 저장이 기본,
  처음부터 그린 것은 새 작업만. 불러오기가 작업(id · 이름 · 종류)을 함께 넘긴다.
- **생성기가 STEP 이 아니라 레시피를 낸다(2026-09-20).** `core/jig_recipe.recipe_of(plan, opts)` 가
  계획을 box · cylinder · pin · bolt · standoff · transform · union 노드로 옮기고, 사람이 바꿀
  치수(`판_길이` · `판_너비` · `판_두께` · `받침_높이` · `받침_지름` · `클램프_패드_지름` ·
  `스페이서_높이` · `롤러_지름` · `스팬` · `노즈_지름` · `틈` · `낙하물_지름`)를 변수로 뺀다.
  자리(받침 · 핀 · 클램프 위치)는 부품에서 나온 수라 그대로. `JigResult.summary()["recipe"]` 에
  실리고 `adopt_jig_run` 이 STEP 대신 이것을 첫 버전으로 넣는다(옛 생성은 STEP 으로). 네 형식
  모두 생성기가 만든 형상과 **같은 상자 · 부피**임을 시험이 잰다. 이제 생성된 지그도 조립 →
  DOE 로 판 두께 · 받침 높이를 바로 훑는다.
- **개인 토큰 화면을 다시 짰다(2026-09-20, ReportArchive 의 MCP 탭을 본떠).** 왼쪽 발급 · 목록 ·
  폐기, 오른쪽 **도구별 등록 방법**(Claude Code — `--transport http` 한 줄 · Claude Desktop —
  mcp-remote 브리지 JSON · Gemini CLI — `httpUrl` + headers · Codex CLI — TOML · 스크립트 — curl).
  토큰이 없어도 형식이 보이고, 발급하면 실제 토큰이 채워지며 발급한 이름(Gemini 등)의 탭이 먼저
  열린다. 만료는 **날짜 칸**(30일 · 90일 · 1년 · 없음 빠른 선택)으로 골라 서버의 「며칠 뒤」 로
  바꿔 보낸다. 이름 빠른 선택, 상태 배지(활성 · 만료 · 폐기됨), 폐기된 것 보기. MCP 주소는
  `GET /auth/mcp-info`(설정 `MCP_PUBLIC_URL`, 없으면 접속 호스트 + 포트 + 2). HTTP 에서도 되는
  복사(`shared/lib/clipboard`).
- **AI 가 그릴 때 필요한 것 여섯(2026-09-20).** AI 는 3D 를 못 보고, 좌표를 세어야 하고, JSON 전체를
  다시 써야 했다. 그 셋을 줄였다:
  1. **그림** `POST /cad/recipe/views` · MCP `recipe_views` — `core/views.py` 가 build123d
     `project_to_viewport` 로 은선 투영(실선 · 점선)한 SVG 를 cairosvg 로 PNG 로. iso · front ·
     top · right. MCP 는 이미지로 돌려줘 Claude 가 눈으로 본다. 의존성 cairosvg(+cairocffi · pillow).
  2. **엣지 · 면 찾기** `POST /cad/recipe/find` · `recipe_find` — `core/recipe/query.find_features`.
     what(edges|faces) · kind · role · of_face_role(그 역할의 가장 넓은 면의 엣지) · axis · radius ·
     길이 · near. 답의 midpoint · center 를 near · plane 에 그대로.
  3. **재기** `POST /cad/recipe/measure` · `recipe_measure` — 선택자(point · hole_near ·
     face_near · edge_near) 둘 사이 거리 · 축별 차 · 평면 각도 · 간격.
  4. **부분 수정** `core/recipe/patch.apply` — set_param · remove_param · add_node · set_field ·
     remove_node(가리키는 것이 있으면 거절) · move_node · rename_node(가리키는 곳도 따라감).
     `POST /cad/recipe/patch`(순수) · `POST /works/{id}/patch`(검사 뒤 새 버전, 틀리면 저장 안 함)
     · MCP `patch_work`.
  5. **표준 부품 노드** `bolt`(머리가 앉는 점 · 호칭 · 길이 · hex|socket · 와셔 · down) · `pin` ·
     `standoff` — 지그 생성기의 볼트 빌더와 같은 비례. 화면 「입체」 그룹에도.
  6. **면에 얹기** `patch.place_on` — 경계 상자로 mover 를 onto 의 면(top|bottom|±x|±y)에 offset
     만큼 띄워, 나머지 축은 center|min|max. `POST /cad/recipe/place` · MCP `place_on`(새 버전 저장).
  가이드 기본 습관에 5~7 을 더했다(눈으로 확인 · 좌표를 짐작하지 않는다 · patch 로 고친다).
- **조립 간섭 검사(2026-09-20).** 지그 생성기 안에만 있던 겹침 검사를 일반화했다 —
  `core/interference.check_pairs` 가 이름표 붙은 형상들의 모든 쌍을 잰다(경계 상자가 안 겹치는
  쌍은 불리언을 안 돌린다). `POST /cad/recipe/interference`(MCP `recipe_interference`)가
  조립(group)의 구성품끼리 겹침을 주고, 조립 편집기는 메시 뒤에 따로 물어 배지 · 「a × b N mm³」
  · 겹친 구성품을 3D 에서 빨갛게 보인다. DOE 는 점마다 `DoePoint.geometry.interference` 에 붙여
  표(「간섭」 열) · manifest.csv(`interference` 열: ok / N건)에 낸다 — 변수를 바꾸다 부품이 판에
  파묻히는 점을 잡는다. 서버는 겹친 채로도 저장한다(막지 않는다) — 보여 주는 것까지.
- **부품 + 지그를 맞는 자리에 놓는 조립(2026-09-20).** `POST /works/assemble` · MCP
  `assemble_jig_on_part(part_source, jig_work_id)` · 내 작업의 「부품 + 지그로 조립」. 두 도면의
  좌표계가 달라(부품은 그린 그대로, 생성된 지그는 부품 XY 중심이 원점 · 판 윗면 z=0 · 부품은
  `product_lift` 만큼 뜸) AI 가 translate 를 손으로 계산해야 했다 — 서버가 지그 작업의 생성
  기록에서 `product_lift` 를 읽어 `(-중심x, -중심y, -바닥z + lift)` 로 놓는다(`placement.mode=
  generated`). 손으로 그린 지그는 규칙이 없어 윗면에 얹어 어림하고 그렇다고 말한다(`guessed`).
  부품 높이는 변수 `부품_높이` 로 들어가 조립을 그대로 DOE 로 훑는다. 「새 조립」 은 「빈 조립」
  으로 이름을 바꿨다.
- **모든 CAD 뷰에 투시/정사영 · 표준 뷰(2026-09-19).** `CameraRig` 가 투시 · 정사영 카메라 한 벌과
  OrbitControls 를 들고, 전환할 때 같은 자리 · 같은 크기로 보이게 옮긴다(투시의 거리 ↔ 정사영의
  zoom). `ViewerToolbar`(등각 · 정면 · 윗면 · 우측 · 투시/정사영)가 `PickViewer` · `GridViewer` ·
  `ModelViewer` 모두의 오른쪽 위에 있다. 형상 보기 · 지그 미리보기는 `useFillHeight` 로 스크롤
  영역(<main>) 아래 끝까지 재서 채운다 — `calc(100vh - N rem)` 은 머리 높이가 조금만 달라도
  페이지 스크롤이 생겼다. DOE 목록은 변수가 많으면 가로로도 스크롤(`min-w-max`).
- **DOE 설계점 형상 보기(2026-09-19).** `GET /doe/{id}/points/{n}/mesh` 가 스냅샷 레시피에 그
  점의 값을 넣어 다시 만든 메시를 준다(STEP 을 읽는 것보다 빠르고 같은 규칙, 최근 64개 캐시).
  화면(`PointsGallery`)은 **하나씩**(표의 줄을 누르거나 ◀ ▶, 카메라는 그대로라 견주기 쉽다) ·
  **겹쳐 보기**(체크한 점들을 한 뷰어에 점마다 색을 달리 — `mergeMeshes` 가 `part` 를 붙인다) ·
  **나란히**(`GridViewer` — **한 WebGL 문맥**에 캔버스 하나를 깔고 칸마다 가위로 잘라 그린다,
  three.js 의 multiple-elements 방식. 카메라 하나라 돌리면 모두 돈다). 점이 수백이라 **고른
  것만** 받고 상한이 있다 — 겹쳐 보기 12(색으로 가를 수 있는 만큼), 나란히 24(그리는 양).
  격자는 정사각형 칸으로 1x1 → 2x1 → 2x2 → 3x2 → 3x3 → 4x3 → 4x4, 그 뒤는 4열로 세로만
  (`gridColumns`). 화면은 왼쪽 도면 · 오른쪽 목록 3:1, 하나씩 볼 때는 목록에 체크가 없고 ◀ ▶
  가 목록 위에, 겹쳐 · 나란히에는 「전체 선택」. 고른 점이 상한을 넘으면 **쪽**으로 나눠 ◀ ▶ 로
  넘겨 가며 다 본다(수백 개도) — 한 번에 그리는 양만 막는 것이지 보는 수를 막는 게 아니다.
  LHS 표본 수 상한도 서버 설정(`doe_max_samples`, 기본 500)으로 — 관리자가 화면에서 바꾼다. `PickViewer.sync`(`cameraSync`)는 뷰어 여럿을
  맞출 때 쓰라고 남겨 두었다.
- **지그 형식 셋을 더했다(2026-09-19).** `JigOptions.kind` 로 규칙을 고른다 — `planning.plan` 이
  형식별 계획 함수로 갈라지고 `FixturePlan` 에 `bolts · rollers · nose · impactor`, `JigElements`
  에 `others`(이름표가 한국말)가 붙었다. 71_AnsysJigAutoModeler(SpaceClaim 툴킷)에서 차용:
  - **볼트 고정**(`bolted`): 수직 관통 구멍 중 서로 먼 것부터 `bolt_max_count` 개에 볼트(ISO 비례
    — 머리 1.5d, 육각/소켓, 와셔, 구멍 지름에서 호칭을 아래로 맞춤 8.5→M8), 판에 탭 구멍,
    `bolt_spacer_height` 면 스페이서. 71 의 Fastener 규칙. 진동 · 충격 시험의 기본 고정.
  - **3점 굽힘**(`bending`): 긴 변 = 스팬 방향, 스팬 = 길이 x 비율 또는 절대값, 롤러 둘(받침대
    위, 폭 + 여유) + 로딩 노즈(줄기). 71 의 BendingFixture 규칙.
  - **낙하 · 충격 자세**(`drop`): `geometry.pose` 가 고른 면/모서리/꼭짓점이 아래를 보게 돌린 뒤
    정규화, 바닥(고정 구멍 없음) + 틈, 낙하물(강구/펜). 71 의 DropTest 규칙.
  화면의 「부품에서 지그 생성」 에 형식 고르기 · 형식별 옵션 · 범례 색이 붙었다. 모터 진동 시험
  지그는 조사 뒤에 — 71 에는 그 형상이 없고 해석 쪽(접촉면 NS · PWM 하중)만 있었다.
- **지그 생성기를 지그 쪽으로 옮겼다(2026-09-19).** 「부품 화면의 지그 만들어 주기 탭」 은 지그를
  만드는 일이 부품에서 시작돼 헷갈렸다. 이제 **「새 작업 › 부품에서 지그 생성」**(`/draw/jig-from-part`):
  부품(내 작업 `work:` · 공용 `part:`)을 고르고 옵션을 주면 `POST /works/jig-from-part` 가 **지그
  작업을 먼저 만들고 그 작업에 생성을 건다**. 끝나면 `POST /works/{id}/jig-runs/{job}/adopt` 가
  결과 STEP 을 첫 버전(`source=generated`)으로 넣고 그 화면으로 간다 — 거기서부터는 그냥 그린다.
  잡는 부품(`jig_for_part_id`)은 공용 부품이거나 승격된 부품이면 이어진다. 부품 쪽의 생성 기록 ·
  `jig-runs` POST · `to-work` · 생성 결과로 승격하는 `promote/jig` 는 지웠다(지그의 승격 길은
  `promote/jig-recipe` 하나). MCP `run_jig(source, options, name)` 이 같은 일을 하고 결과가 첫 버전이
  된 채로 돌아온다, `promote_jig` 는 뺐다. 지그 작업 화면은 생성 기록이 있으면 「생성기 결과 —
  계획 · 간섭」 카드로 되짚어 보고, 아직 도면이 없으면 「도면으로 가져오기」 를 준다.
  **미리보기**: 옵션 서른 개를 글로 읽고 상상하게 하지 않는다 — 부품을 고르면 같은 규칙이 바로
  돌아(`POST /works/jig-from-part/preview`, `pipeline.analyze` — 내보내기 전까지) 제품 + 바닥판 ·
  받침 · 위치 핀/받침대 · 클램프가 **종류별 색**으로 3D 에 뜨고, 옵션을 바꾸면 따라 바뀐다. 범례가
  각 요소를 부품의 무엇에서 정하는지(바닥면 · 구멍 · 윗면) 말하고, 누르면 그 요소만 또렷하다.
  MCP `jig_preview`.
- **DOE 를 손봤다(2026-09-19, 사용자 요청 넷).** 화면 이름을 「실험계획」 에서 **「DOE」** 로.
  만들기는 **서버 보관 폴더**(`filestore/doe/…`, `DoeStudy.local_dir`)에 하고, 다 끝난 뒤
  **「공유 폴더로 보내기」**(`POST /doe/{id}/export`, MCP `doe_export`)가 복사한다 — 해석이 읽는
  폴더에 만들다 만 것이 보이지 않게. `export_dir` · `exported_at` 은 보낸 뒤에만 찬다(0010,
  이미 만든 것은 보낸 것으로 친다). **「설정 바꿔 다시 만들기」** — `/doe/new?from=<id>` 가 지난
  DOE 의 대상 · 이름 · 방식 · 인자를 채워 새 DOE 를 만든다(지난 것은 그대로, 대상이 지워졌으면
  스냅샷으로). 「지그 만들어 주기」 탭은 부품 작업의 덤으로 아직 있다 — 사용자가 없앤 줄 알고
  있어 물어봤다.
- **조립에 미리보기를 넣었다(2026-09-19).** 저장해야 3D 를 볼 수 있어 자리를 숫자로 넣고
  저장해 확인하던 것을 없앴다. 왼쪽은 **구성**(놓인 것들의 목록 — 색점 · 이름 · 출처, 손을 올리면
  「편집」 · 「빼기」) 과 **가져오기**, 가운데는 3D — 가져오는 순간 그려진다. 자리 · 회전 · 치수
  덮어쓰기는 「편집」 이 여는 창(모달 아님, 오른쪽에 붙음)에서 고치고, 고치는 대로 3D 가 따라온다.
  목록에서 고르면 그 구성품만 또렷하고 나머지는 반투명. 그러려고 평가기가 group 의 자식에
  구성품 id 를 이름표로 붙이고(`_to_part` 가 그것을 보존), 메시가 면 · 엣지에 `part` 를 준다 —
  뷰어의 `partColors` · `emphasis`. 도면 편집기의 미리보기 루프는 `useRecipeMesh` 훅으로 빼서
  둘이 같은 길을 쓴다.
- **「그리기」 를 「새 작업」 으로(2026-09-19).** 저장은 늘 새 작업만 만들고, 덮어 저장은 내 작업 ›
  수정에서. 내 작업의 단추도 두 줄로 — 머리는 작업의 일(실험계획 · 승격 · 지우기), 도구줄은
  도면의 일(수정 · STEP 받기 · 템플릿으로 저장). STEP 올리기는 「수정」 안 「파일」 탭으로.
  도면 없이 만든 작업(빈 조립)이 commit 되지 않아 만든 직후 사라지던 버그를 잡았다.
- **업무 흐름을 다시 세웠다(구조 4차, 사용자와 확인한 것)**:
  ```
  그리기 ─저장─▶ 부품 · 지그 (도면 + 변수 심기까지. 여기서 끝)  ─승격─▶ 공용 부품 · 공용 지그
                └ 새 조립 ▶ 조립 (부품 · 지그를 가져다 위치)
  실험계획(독립 공간) ▶ 대상을 고른다(부품 · 지그 · 조립 중 저장된 것) ▶ 변수에 범위 · 목록 ▶
                      인스턴스 여럿 ▶ 공유 폴더
  템플릿 라이브러리(공용 공간) ▶ 부품 · 지그를 그릴 때 출발점
  ```
  - 부품 · 지그 · 조립 화면에서 **실험계획 탭을 뺐다**. 머리에 「실험계획 만들기」 만 남겨 대상으로
    넘겨 준다(`/doe/new?work=…`). 실험계획은 `/doe/new` 에서 **대상을 먼저 고른다**.
  - 실험계획 목록이 대상(이름 · 종류)을 적는다.
  - 말: 「레시피」 → **「도면」**(JSON 키 · API 는 그대로), 「레시피 불러오기」 → 「템플릿 불러오기」,
    「모아 둔 것」 → 「공용 공간」, 「부품 카탈로그로 승격」 → 「공용 부품으로 승격」, 「레시피
    고치기」 → 「수정」.
- **조립을 만들었다(구조 3차).** 「부품 탭 · 지그 탭」 으로 한 작업 안에 둘을 엮던 것을 풀었다:
  부품과 지그는 **서로 관계없는 각자의 도면**이고, 둘을 잇는 자리는 새 종류 **`assembly`** 다.
  - 피처 둘: `component`(다른 도면을 **살아 있는 레시피로** 가져와 `params` 덮어쓰기 · 자리 ·
    회전)와 `group`(붙이지 않고 묶는다 — 부품과 지그는 따로 남아야 본다 · 뺀다 · 잰다).
    `resolve_component` 가 `part:` · `jig:` · `work:` 를 푼다(코어는 DB 를 모른다).
  - 화면(처음 모습 — 위 「조립에 미리보기」 로 바뀜): 왼쪽 라이브러리(내 도면 · 부품 · 지그 카탈로그)
    + 조립 변수 판, 오른쪽에 놓인 것들과
    자리 · 회전 · **구성품 치수 덮어쓰기**. 모든 칸에 `fx` 가 있어 조립 변수로 물린다 —
    그래야 조립을 실험계획으로 훑는 뜻이 있다.
  - 탭: 조립 작업은 「조립 · 실험계획」, 그리는 작업은 「도면 · 실험계획」(+ 부품이면 지그 생성기).
    탭 이름이 종류에 따라 바뀌던 혼란을 없앴다.
  - 저장 단추는 「저장」 하나이고 종류는 대화상자에서 고른다. 작업은 첫 버전 없이도 만들 수 있다
    (빈 조립 → 구성품을 놓으면서 첫 버전이 생긴다).
  - 같은 **덮어쓰기 버그**가 조립에서 또 났다(변수를 만들자마자 사라짐) → `useRecipeEmit` 훅으로
    빼서 레시피를 고치는 화면이 모두 같은 길을 쓴다.
- 고친 것(쓰는 흐름): 종류 고르기를 **저장 대화상자 안에 숨겨** 두었더니 아무도 못 찾았다 —
  그리기 화면 위에 「지금 그리는 것: 부품 | 지그」 를 두고, 저장 단추 이름도 그것을 따른다
  (「지그로 저장」). 내 작업 목록에 **종류 거르개**(전체 · 부품 · 지그)를 두어 「내 지그가 어디
  있지」 를 없앴다. 지그 생성기는 옵션 서른 개를 펼쳐 두던 것을 접고, **무엇을 해 주는지 한 줄**
  과 「지그 만들어 보기」 단추를 앞에 뒀다(세부 옵션은 펴서 본다).
- 더한 것: 생성기 결과를 **「이어서 그리기」** — 결과 STEP 을 `import_step` 한 줄짜리 **지그
  작업**으로 가져온다. 생성기는 출발점을 만들어 줄 뿐이고, 받침을 옮기거나 튜닝부를 붙이는
  일은 사람이 한다. 가져오면 그때부터 변수 · 실험계획이 그대로 붙는다.
- **구조를 고쳤다(작업의 종류)**: 「같은 레시피가 부품도 되고 지그도 되는」 애매함을 없앴다.
  작업에 `kind`(part · jig)를 두고 **그리는 방법은 하나로 통일**했다 — 레시피 · 변수 · 실험계획은
  둘이 똑같이 쓴다. 다른 것은 (1) 그림 탭의 이름, (2) **어느 카탈로그로 올라가나**, (3) 덤으로
  쓰는 도구뿐이다: 부품 작업엔 **지그 생성기**(제품에서 받침 · 핀 · 클램프를 규칙으로), 지그
  작업엔 **잡는 부품**(이어 두면 승격까지 따라간다). 「그린 지그로 승격」 이라는 애매한 단추는
  없앴다 — 승격은 종류를 따라간다. `0008_work_kind`.
- 고친 것: 「그린 지그로 승격」 을 **부품 탭**에 두었더니 못 찾았다 — 지그 일은 지그 탭에서
  찾는다. 지그 탭 맨 위에 「이 작업의 레시피를 지그로」 칸을 두고, 그 아래 생성 옵션의 제목을
  「제품에서 만들어 주기」 로 바꿔 **두 길**이 한눈에 보이게 했다.
- 더한 것: **그린 지그**. 「왜 지그에는 변수를 못 심나」 의 답은 구조였다 — 부품은 **그리는 것**
  (레시피 → 변수 · DOE)이고 지그는 **만들어 주는 것**(생성기 + 옵션)이라 그릴 자리가 없었다.
  그런데 공진 튜닝 지그처럼 생성기가 만들 수 없는 지그가 있다. 그런 지그는 레시피로 그리고
  `promote_jig_recipe`(`POST /works/{id}/promote/jig-recipe`)로 지그 카탈로그에 올린다 —
  그리는 것이니 변수 · DOE 가 그대로 따라온다. 지그 결과 화면은 제품 없이 지그만 있어도 그린다
  (`jig_glb` 가 없으면 레시피 평가의 `model_glb`).
- 고친 것: DOE 탭이 **저장된 버전**을 본다는 것을 화면이 말하지 않았다 — 편집기에서 변수를
  만들고 저장하지 않은 채 오면 「변수가 없습니다」 만 뜬다. 이제 탭 머리에 「기준: 저장된 v3 ·
  변수 2개(두께, 길이)」 를 적고, 저장 안 된 고침이 있으면 붉은 줄로 알린다(「부품 탭으로」).
  그 단추는 **이미 고치는 중이면 편집기를 다시 시작하지 않는다** — 적어 둔 것이 날아간다.
- 고친 것(버그): 칸에서 만든 변수가 **곧바로 사라졌다.** 한 동작이 레시피를 두 번 고치는데
  (변수 만들기 → 칸을 `=이름` 으로) 둘 다 `value` 프롭에서 새 레시피를 만들어, 두 번째가 아직
  다시 그려지기 전의 낡은 값 위에서 만들어져 첫 번째를 덮었다. 편집기의 모든 갱신을 **방금
  내보낸 것 위에 쌓는** `emit()` 하나로 모았다(되돌리기 · JSON · 불러오기도 같은 길).
  시험 둘로 못 박았다 — 피처 칸과 스케치 도형 칸.
- 고친 것: 빨간 `?` 가 까닭을 안 말했다. 「아직 안 썼다(`=`)」 까지 오류처럼 보였다 —
  이제 셋을 가른다: **쓰는 중**(옅은 `…`) · **없는 변수**(빨강 + 「이 이름으로 만들기」) ·
  **읽을 수 없는 식**(빨강 + 괄호를 보라). 칸 아래 한 줄로 적는다.
- 더한 것: **맞서는 목표**(파레토). 「두께를 키우면 공진은 목표에 가까워지지만 질량이 는다」
  같은 자리에서 **가중치를 묻지 않는다** — 그 답은 가중치의 것이 된다. 아무한테도 지지 않는
  점만 가려 표와 흩뿌림으로 보여 주고 고르는 일은 사람에게 맡긴다(`core/doe.pareto`,
  `POST /doe/{id}/tradeoff`, MCP `doe_tradeoff`). 값이 없는 점은 견주지 않는다(`comparable`).
  변수끼리는 원래 안 싸운다 — 칸 하나에 식 하나라 과구속이 없다. 부딪히는 것은 형상이고,
  그런 점은 사유와 함께 그 점만 실패로 남는다.
- 남긴 것: 모달 해석(ANSYS 로 보낸다 — 이 플랫폼은 DOE 와 형상까지) · 메시 · 결과 되읽기 ·
  스케치 구속 솔버 · 실루엣 · 도면 치수선(DXF 주석).

## 4단계. AI 로 추가 — MCP 서버 ✅ 완료 (2026-09-18, 방향 바꿈)

**플랫폼이 AI 를 부르지 않는다 — AI 가 플랫폼을 도구로 쓴다.** Claude API 대신 MCP 서버
(`mcp_server/`)를 두고, 각 AI 클라이언트(Claude Code · Desktop · 다른 것)가 사용자의 **개인
토큰(PAT)** 으로 붙는다. 검증 · 권한은 백엔드가 한다.

- 도구 21개, 흐름 단위로 고정: `get_guide` · `recipe_schema` · `recipe_check`(**만들어 보고**
  실패하면 어느 노드가 왜인지 — AI 의 자기 수정 루프) · 내 작업 읽기/만들기/버전 저장(출처 "ai")/
  되돌리기 · `run_jig`(끝날 때까지 기다려 계획 · 간섭을 돌려줌) · 승격(사용자가 시킬 때만) ·
  카탈로그 읽기 · 내 공간으로 복사.
- 가이드는 서버가 쥔다(`mcp_server/guide/GUIDE.md`) — 레시피 규칙 · 자주 하는 실수 · 작업 순서.
- PAT: 「내 정보」 에서 발급, 범위 `read` · `write`. 검증 · 미리보기 POST 는 읽기로 친다.
- 개발에서는 `run.py` 가 MCP 서버(8062)를 자식으로 띄운다. FastMCP 는 리스트 결과를 content 로
  쪼개므로 도구는 늘 dict 하나를 돌려준다(실측).
- 완료 기준: Claude Code 에 등록하고 "구멍판에 모서리 R3" 를 시키면 내 작업에 출처 AI 버전이
  생기고 화면에서 3D 로 보인다 → 진짜 MCP 클라이언트로 create → version → jig → promote 확인.
- 남긴 것: 「후보 → 적용」 UI(지금은 AI 가 저장한 것이 곧 새 버전이고, 마음에 안 들면 되돌리기).
  STEP 업로드 도구(MCP 로 파일을 보내는 클라이언트가 생기면).

## 5단계. 공용 공간 — 게시와 라이브러리 ✅ 앞당겨 완료 (2026-09-18, 위 「구조」)

- 된 것: 내 작업(비공개) → 승격 → 부품 · 지그 카탈로그(불변 버전) · 내 공간으로 복사.
- 남긴 것: 검색 · 태그, 부서 단위 공개 범위(StandardPlatform 의 workspace)는 필요해질 때.

## 6단계. 외부 API (PAT 는 4단계에서 먼저 들어옴)

- 개인 토큰(PAT) — StandardPlatform 의 것을 가져온다(범위 read · models:write · jobs:write).
- 공개 API: 게시된 모델 목록 · STEP 내려받기 · 레시피 → STEP · 지그 생성 · 작업 조회. 웹훅.
- 완료 기준: `curl -H "Authorization: Bearer autojig_pat_…"` 로 STEP 을 받고 작업을 건다.

## 병행. 지그 코어 심화

규격 부품 라이브러리(클램프 · 핀 · 받침 STEP 을 5단계 라이브러리에 게시 → 계획이 배치) ·
특징 인식 확장(경사면 · 옆 구멍 · 포켓) · 지그 편집(받침을 옮기면 간섭 재검사).

## build123d 를 얼마나 쓰고 있나 (2026-09-19 실측)

`app/` 에서 쓰는 이름을 build123d 의 모델링 모듈과 맞춰 센 것. 재보는 법은
`python - <<'EOF'` 로 `importlib` + 정규식 — 아래 표는 그 결과다.

| 모듈 | 쓰는 것 / 전체 | 안 쓰는 것 중 값어치 |
| --- | --- | --- |
| objects_curve (선 · 호) | 8 / 31 (26%) | CenterArc · JernArc — 중심 · 사이각으로 그리는 호 |
| objects_sketch (2D) | 12 / 14 (86%) | SlotArc(곡선 슬롯 — `path` 로 대신한다) |
| objects_part (3D 기본) | 6 / 11 (55%) | Hole · CounterBoreHole(우리는 hole 노드로 직접 만든다) · ConvexPolyhedron |
| operations_generic | 9 / 10 (90%) | project(실루엣 — 곡면에서 어긋난다, 실측) |
| operations_part | 6 / 8 (75%) | thicken(면을 두껍게 — 평면이면 돌출과 같다) |
| operations_sketch | 2 / 4 (50%) | trace(우리 `path` 와 같은 일 — 직접 구현) · full_round |
| importers | 1 / 5 (20%) | import_stl · import_svg(로고 · 외곽선 가져오기) |
| exporters (2D) | 2 / 11 (18%) | Drawing · LineType · ColorIndex — **도면 치수선 · 선종류** |
| exporters3d | 3 / 5 (60%) | export_brep |
| **합계** | **48 / 99 (48%)** | |

쓰는 쪽이 적어 보이지만 안 쓰는 것의 대부분은 **한 가지를 그리는 여러 방법**(호를 접선 · 반지름 ·
사이각 … 으로 그리기)이다. 레시피는 그 중 하나만 고르면 된다. 다음에 값어치가 큰 것은 표의 굵은
글씨 — 도면 치수선(exporters 의 Drawing)과 실루엣(HLR).

2026-09-19 넷째 묶음에서 **AI 가 말로 받은 치수를 그대로 옮길 수 있는 것**(반지름 · 접선 호,
중심 사이 장공, 변 · 각 삼각형, 판금 절곡)을 먼저 채웠다 — 좌표로 환산하다 틀리는 일이 제일 많다.

## 순서의 이유

- 1 → 2: 큐 없이 레시피 평가를 붙이면 AI 편집(수십 초)에서 다시 뜯는다.
- 2 → 3 · 4: 그리기와 AI 는 같은 레시피를 다룬다. 3 과 4 는 바꿔도 되지만 폼 편집기(3-1)는
  AI 보다 먼저 — AI 결과를 사람이 고칠 수 있어야 한다.
- 5 → 6: 외부에 여는 것은 게시된 것뿐이다. 내 공간의 작업 중인 것이 API 로 새면 안 된다.
